{
  "name": "weekcrm",
  "version": "2.0.0",
  "description": "WeekCRM — CRM news, reviews, and vendor directory",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^15.0.0"
  }
}
